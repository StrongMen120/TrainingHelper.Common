apiVersion: v1
kind: ConfigMap
metadata:
  name: training-helper-app-cm
  labels:
    app: training-helper-app
data:
  config.json: |
    {
        "authentication": {
            "domain": "",
            "clientId": "",
            "audience": ""
        }
    }
